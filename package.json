{
  "name": "aker-website",
  "version": "1.0.0",
  "private": true,
  "description": "AKER OSGB kurumsal web sitesi - statik site, Cloudflare Pages",
  "type": "module",
  "scripts": {
    "build": "npm run bundle && npm run pages",
    "bundle": "node tools/bundle.ts",
    "pages": "node tools/build.ts",
    "typecheck": "tsc --noEmit",
    "check": "node tools/seo-check.ts",
    "verify": "npm run typecheck && npm run build && npm run check",
    "dev": "node tools/serve.ts",
    "db:seed": "node tools/seed.ts > db/seed.sql",
    "db:setup": "wrangler d1 execute aker-website --local --file=db/schema.sql && wrangler d1 execute aker-website --local --file=db/seed.sql",
    "db:setup:remote": "wrangler d1 execute aker-website --remote --file=db/schema.sql && wrangler d1 execute aker-website --remote --file=db/seed.sql",
    "dev:full": "wrangler pages dev . --port 8788",
    "e2e": "node tools/e2e.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250801.0",
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
